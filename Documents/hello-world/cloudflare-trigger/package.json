{
  "name": "testforge-trigger",
  "version": "1.0.0",
  "description": "Cloudflare Worker — triggers TestForge test runs via GitHub Actions",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
